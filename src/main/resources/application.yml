# Default configuration for the OpenShelves

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /

# Spring Configuration
spring:
  application:
    name: openshelves-api

# Logging Configuration
logging:
  level:
    root: INFO
    com.openshelves: DEBUG
