# Default configuration for the OpenShelves Core application.

# Server Settings
server:
  port: 8080
  servlet:
    context-path: /

# Spring Application Settings
spring:
  application:
    name: openshelves-core

# Logging Configuration
logging:
  level:
    root: INFO
    com.openshelves: DEBUG
