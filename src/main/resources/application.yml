# -----------------------------------------
# Application Configuration
# -----------------------------------------

# Server Configuration
server:
  port: 8080

# Spring Configuration
spring:
  application:
    name: katalog-api
